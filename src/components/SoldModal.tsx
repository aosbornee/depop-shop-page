import React from 'react';
import './SoldModal.css';

const SoldModal = () => {
  return (
    <div className="sold-modal" data-testid="sold-modal">
      <p className="sold-text">Sold</p>
    </div>
  );
};

export default SoldModal;
